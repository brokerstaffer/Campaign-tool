# EmailBison API findings

Generated by `scripts/probe-eb.mjs` against `https://send.brokerstaffer.com`.

Probe range: `2026-07-01` .. `2026-07-30`.

This file is checked in deliberately. It is the answer sheet for the open
questions in the plan — re-run the probe rather than re-deriving them.

## Summary

| Question | Verdict | Detail |
|---|---|---|
| Q1 workspace count | 2 visible | 2: BrokerStaffer's Team, 3: Eddy's Team |
| Q17 campaign names | 95 campaigns | statuses seen: active, paused, completed |
| Q8b status vocabulary | active | paused | completed |  |
| Q6 single-day step stats | present (3 steps) | 30d range returned 3 steps |
| Q5 total_leads_contacted | 30d=0, 1d=0 | compare against SUM of daily values once backfilled |
| Q7 variants | 3 steps, 0 flagged variant | no variants on this campaign — probe another |
| Q8 spintax | none found in this body | try a campaign known to use spintax |
| Replies total | 1915587 |  |
| Human/Positive fields | automated_reply=true, interested=true | these back Human Replies and Positive |
| Q16 message direction | folder, type, sender_email_id | older=0, newer=0 |
| Sender emails | 1470 |  |

## Q1 — Workspaces visible to the token

```json
[
  {
    "id": 2,
    "name": "BrokerStaffer's Team",
    "personal_team": true,
    "main": true,
    "parent_id": null,
    "total_monthly_email_verification_credits": 0,
    "remaining_monthly_email_verification_credits": 0,
    "remaining_email_verification_credits": 0,
    "total_email_verification_credits": 0,
    "sender_email_limit": 100000,
    "warmup_limit": 100000,
    "warmup_filter_phrase": "wmpmyseb",
    "has_access_to_warmup": false,
    "has_access_to_healthcheck": false,
    "created_at": "2026-03-30T17:47:53.000000Z",
    "updated_at": "2026-07-19T03:09:02.000000Z"
  },
  {
    "id": 3,
    "name": "Eddy's Team",
    "personal_team": true,
    "main": true,
    "parent_id": null,
    "total_monthly_email_verification_credits": 0,
    "remaining_monthly_email_verification_credits": 0,
    "remaining_email_verification_credits": 0,
    "total_email_verification_credits
… truncated
```

**More than one workspace.** Every sync must either fan out or pin a team id, and `teams` is a real dimension rather than a constant.

## Q17 — Campaign names (for the client matcher)

Total: **95**. Statuses observed: `active`, `paused`, `completed`.

The matcher must be pinned by unit tests against these exact names before it
decides how the reporting is grouped.

```
   128  Properties & Estates + Nicole + Florida
   127  RE/MAX Pacific 2 + Nicole + SOCAL
   126  Rise Real Estate Antelope New Target 2 + Nicole + SOCAL
   125  Douglas Elliman LA + Nicole + Beverly Hills
   124  Copy of Discover Flag Team 6 + Nicole + ARMLS
   123  Douglas Elliman NYC + Nicole +REBNY
   122  Douglas Elliman Las Vegas + Nicole + GGVAR
   121  FAST Real Estate + Nicole + BRIDGE
   120  Douglas Elliman LA + Nicole + SOCAL
   119  MattC Group 3 + Nicole + Client List
   118  Discover Flag Team 5 + Nicole + ARMLS
   117  Howe Realty 2 + Nicole + ARMLS
   116  Bastion Realty South 3 + Nicole + Florida
   115  Bastion Realty South 3 + Nicole + Alabama
   114  Jeff Cook Real Estate LPT Realty 2 + Nicole + Moncks Corner, Summerville, Ladson, Goose Creek, Ridgeville, Hanahan
```

## Campaign row shape

```json
{
  "id": 128,
  "uuid": "a26250f4-ccd2-4353-b112-a9ea1803343a",
  "sequence_id": 116,
  "name": "Properties & Estates + Nicole + Florida",
  "type": "outbound",
  "status": "active",
  "completion_percentage": 0,
  "emails_sent": 0,
  "opened": 0,
  "unique_opens": 0,
  "replied": 0,
  "unique_replies": 0,
  "bounced": 0,
  "unsubscribed": 0,
  "interested": 0,
  "total_leads_contacted": 0,
  "max_emails_per_day": 2000,
  "max_new_leads_per_day": 2000,
  "plain_text": true,
  "open_tracking": false,
  "total_leads": 236,
  "can_unsubscribe": false,
  "unsubscribe_text": null,
  "include_auto_replies_in_stats": true,
  "sequence_prioritization": "followups",
  "created_at": "2026-07-30T17:11:27.000000Z",
  "updated_at": "2026-07-30T17:50:17.000000Z",
  "tags": []
}
```

## Q6 — Does a single-day range return step stats?

30-day range: **3** entries in `sequence_step_stats`.

Single day (`2026-07-30`): **3** entries.

Single-day ranges work. `campaign_day_stats` and `campaign_step_stats_daily`
can both be filled by one call per campaign per day, and any range is a SUM.

### Range response
```json
{
  "emails_sent": 0,
  "total_leads_contacted": 0,
  "opened": 0,
  "opened_percentage": 0,
  "unique_opens_per_contact": 0,
  "unique_opens_per_contact_percentage": 0,
  "unique_replies_per_contact": 0,
  "unique_replies_per_contact_percentage": 0,
  "bounced": 0,
  "bounced_percentage": 0,
  "unsubscribed": 0,
  "unsubscribed_percentage": 0,
  "interested": 0,
  "interested_percentage": 0,
  "sequence_step_stats": [
    {
      "sequence_step_id": 354,
      "email_subject": "Hiring Agent for Zillow Preferred leads",
      "sent": 0,
      "leads_contacted": 0,
      "unique_opens": 0,
      "unique_replies": 0,
      "unsubscribed": 0,
      "bounced": 0,
      "interested": 0
    },
    {
      "sequence_step_id": 355,
      "email_subject": "Re: Hiring Agent for Zillow Preferred leads",
      "sent": 0,
      "leads_contacted": 0,
      "unique_opens": 0,
      "unique_replies": 0,
… truncated
```

## Q5 — What does `total_leads_contacted` count?

Over 30 days: **0**

Over a single day: **0**

This is the Prospects metric. The open question is whether it counts leads
contacted DURING the range, or leads FIRST contacted during it. If the former,
summing daily values overcounts (a lead emailed twice counts twice) and the
chart legitimately will not sum to the KPI — which is the documented exception.

## Q7 / Q8 — Sequence step model and spintax

3 steps, 0 flagged as variants.

No spintax found in the first step's body. Probe a campaign known to use it
before writing the parser.

```json
{
  "id": 354,
  "active": true,
  "email_subject": "Hiring Agent for Zillow Preferred leads",
  "order": 1,
  "email_body": "<p><span style=\"color:rgb(65,65,65);font-family:Averta, sans-serif;font-size:16px;font-style:normal;font-weight:400;letter-spacing:normal;text-align:left;text-indent:0px;text-transform:none;word-spacing:0px;white-space:normal;background-color:rgb(255,255,255);text-decoration-thickness:initial;text-decoration-style:initial;text-decoration-color:initial;float:none;\">Hey {FIRST_NAME},</span><br style=\"font-family:Averta, sans-serif;color:rgb(65,65,65);font-size:16px;font-style:normal;font-weight:400;letter-spacing:normal;text-align:left;text-indent:0px;text-transform:none;word-spacing:0px;white-space:normal;background-color:rgb(255,255,255);text-decoration-thickness:initial;text-decoration-style:initial;text-decoration-color:initial;\"><br style=\"font-family:Aver
… truncated
```

## Reply row shape

Total replies: **1915587**

`automated_reply` present: **true** (backs "Human Replies")
`interested` present: **true** (backs "Positive")

```json
{
  "id": 1915648,
  "uuid": "a2625ee0-1b25-431b-8eb6-b9abe7992f39",
  "folder": "Inbox",
  "subject": "How do you like the new website? | M3K61C6 CWSDD6Y",
  "read": false,
  "interested": false,
  "automated_reply": false,
  "html_body": "<div style=\"overflow: auto;\"><p>Howdy Nicole,</p><p>Congrats on the new job!</p><p>I'd love to get your feedback - feel free to email me anytime or reply here to let me know how we're doing. Thanks again for being part of this project.</p><p>I'll ask John to send us a reminder next week.</p><p>Sincerely,<br>Violet Williams</p><p>M3K61C6 CWSDD6Y</p></div>",
  "text_body": "Howdy Nicole,\n\nCongrats on the new job!\n\nI'd love to get your feedback - feel free to email me anytime or reply here to let me know how we're doing. Thanks again for being part of this project.\n\nI'll ask John to send us a reminder next week.\n\nSincerely,\nViolet Williams\n\n
… truncated
```

## Q16 — Message direction in a conversation thread

`older_messages`: 0, `newer_messages`: 0

Candidate direction fields: `folder`, `type`, `sender_email_id`

This decides whether Median Reply Time and Median Follow-up Time can be
derived at all. If no field reliably marks our own messages, the fallback is
matching `from_email_address` against the synced `sender_emails`.

```json
{
  "id": 1915648,
  "uuid": "a2625ee0-1b25-431b-8eb6-b9abe7992f39",
  "folder": "Inbox",
  "subject": "How do you like the new website? | M3K61C6 CWSDD6Y",
  "read": false,
  "interested": false,
  "automated_reply": false,
  "html_body": "<div style=\"overflow: auto;\"><p>Howdy Nicole,</p><p>Congrats on the new job!</p><p>I'd love to get your feedback - feel free to email me anytime or reply here to let me know how we're doing. Thanks again for being part of this project.</p><p>I'll ask John to send us a reminder next week.</p><p>Sincerely,<br>Violet Williams</p><p>M3K61C6 CWSDD6Y</p></div>",
  "text_body": "Howdy Nicole,\n\nCongrats on the new job!\n\nI'd love to get your feedback - feel free to email me anytime or reply here to let me know how we're doing. Thanks again for being part of this project.\n\nI'll ask John to send us a reminder next week.\n\nSincerely,\nViolet Williams\n\n
… truncated
```

## Sender email row shape

```json
{
  "id": 1536,
  "name": "Nicole Collins",
  "email": "nicole@realtypartnersnow.com",
  "email_signature": null,
  "imap_server": null,
  "imap_port": null,
  "smtp_server": null,
  "smtp_port": null,
  "daily_limit": 10,
  "type": "google_workspace_oauth",
  "status": "Connected",
  "warmup_enabled": true,
  "tags": [
    {
      "id": 13,
      "name": "Google",
      "default": true,
      "created_at": "2026-03-30T17:47:53.000000Z",
      "updated_at": "2026-03-30T17:47:53.000000Z"
    }
  ],
  "emails_sent_count": 0,
  "total_replied_count": 0,
  "total_opened_count": 0,
  "unsubscribed_count": 0,
  "bounced_count": 0,
  "unique_replied_count": 0,
  "unique_opened_count": 0,
  "total_leads_contacted_count": 0,
  "interested_leads_count": 0,
  "created_at": "2026-07-23T21:08:30.000000Z",
  "updated_at": "2026-07-30T16:55:38.000000Z"
}
```

## Workspace range aggregate (Prospects KPI source)

```json
{
  "emails_sent": 158816,
  "total_leads_contacted": 67876,
  "opened": 0,
  "opened_percentage": 0,
  "unique_opens_per_contact": 0,
  "unique_opens_per_contact_percentage": 0,
  "unique_replies_per_contact": 2207,
  "unique_replies_per_contact_percentage": 3.25,
  "bounced": 3376,
  "bounced_percentage": 4.97,
  "unsubscribed": 0,
  "unsubscribed_percentage": 0,
  "interested": 7,
  "interested_percentage": 0.32,
  "sequence_step_stats": []
}
```

---

# Manual investigation — 2026-07-30

Findings the automated probe surfaced but could not interpret. **Read this
before writing migrations 003 and 005.**

## Workspaces (Q1) — one that matters

Two are visible: `2 BrokerStaffer's Team` and `3 Eddy's Team`. Eddy's Team is
**empty** (0 campaigns, 0 sent, 0 replies). All data is in workspace 2, so
`team_id` stays effectively constant and **no switchWorkspace fan-out is needed**.

⚠️ `POST /api/workspaces/v1.1/switch-workspace` mutates server-side state on the
**token**, not per-request. Anything that switches MUST switch back, or every
other consumer of this key starts reading the wrong workspace.

## Q3 — timezone: series and range aggregate agree, nearly

30-day window `2026-07-01 .. 2026-07-30`, workspace-wide:

| Metric | Daily series sum | Range aggregate | Delta |
|---|---|---|---|
| Sent | 158,915 | 158,816 | **+99** (0.06%) |
| Replied | 2,291 | 2,207 `unique_replies_per_contact` | **+84** |
| Bounced | 3,376 | 3,376 | 0 ✓ |
| Interested | 7 | 7 | 0 ✓ |

Exactly 30 points for a 30-day inclusive range, first point == requested start.
No off-by-one, so **no timezone conversion is needed for `stat_date`**.

**Q4 answered:** the series' `Replied` is TOTAL replies; the range aggregate's
`unique_replies_per_contact` is UNIQUE per lead. They are different metrics, not
a discrepancy. The 99-send delta is small and unexplained — reconcile again after
backfill before trusting Sent to the unit.

## Q5 — `total_leads_contacted` is DISTINCT leads in the range

Campaign 117: 30d `sent=5,449 / prospects=4,289`; single day
`sent=997 / prospects=997`. Sent exceeds prospects over a range but equals it
within a day. So it is **distinct leads contacted during the range**, and
summing daily values OVERCOUNTS. The chart legitimately will not sum to the
Prospects KPI — this is the documented exception, not a bug.

## Q6 — single-day ranges work ✓

`POST /api/campaigns/117/stats {start=end=2026-07-29}` returned all three
`sequence_step_stats` with real numbers. **`campaign_day_stats` and
`campaign_step_stats_daily` are both viable from one call per campaign per day.**

## Q7 — there are NO variants

Zero steps flagged `variant` across every sampled campaign. The
Campaign → Variant → Step hierarchy collapses to **Campaign → Step** in practice,
and the medal/winner ranking has nothing to rank. Build the third level, but
expect it to be empty.

## Q8 — spintax is `{a | b | c}`

Confirmed, ~7 groups per email, spaces around the pipe:
`{Quick question, | Just a quick question, | I have a quick question,}`

## ⚠️ `/api/replies` is the MASTER INBOX, not campaign replies

| folder | rows |
|---|---|
| inbox | 928,830 |
| sent | 959,389 |
| bounced | 18,175 |
| spam | 9,412 |
| **all** | **1,915,806** |

A sampled inbox row was `type: "Untracked Reply"`, `tracked_reply: false`,
`campaign_id: null`, `lead_id: null`. **Do not sync this endpoint.**

Use **`GET /api/campaigns/{id}/replies`** instead — campaign-scoped, all rows
`type: "Tracked Reply"` with real `campaign_id` and `lead_id`. Campaign 117 has
109. Across ~2,300 replies/30d over 95 campaigns this is trivially syncable.

## ⚠️ `interested` is effectively unused — Positive needs our own classifier

`GET /api/replies?status=interested` returns **30 rows for all time**, and the
30-day workspace aggregate reports `interested: 7`. The reference product shows
**Positive = 389** for a comparable window.

**Q14 answered: `replies.interested` cannot produce "Positive".** Either the
reference classifies replies itself, or Positive means something else entirely.
This blocks Positive, Positive Rate and Lead-to-Email — three of the twelve KPIs.

## Reply timing — one metric is cheap, the other is not

`GET /api/replies/{id}/conversation-thread` returned
`older_messages: 0, newer_messages: 0` for **all 15** tracked replies tested.
**It cannot source either timing metric.** (This invalidates the mechanism
proposed in the plan.)

Working alternatives:

- **Median Reply Time — viable and cheap.** `GET /api/leads/{lead_id}/sent-emails`
  returns sends with `campaign_id`, `sequence_step_id`, `sent_at`. Lead 65642:
  first send `2026-07-27T20:29:52`, reply `2026-07-30T17:43:20` → 2.9d.
  One call per replying lead (~2,300/30d).
- **Median Follow-up Time — viable but expensive.** Our outbound replies live in
  `folder=sent` as `type: "Outgoing Email"` with `campaign_id: null` and
  `lead_id: null` — but they DO carry `primary_to_email_address`. So it requires
  matching sent rows to lead emails by address over a 959K-row folder.
  **Needs a decision before building.**

## The send-fact feed exists: `/api/scheduled-emails`

`?status=sent` → **207,167** rows carrying `campaign_id`, `lead_id`,
`sequence_step_id`, `sent_at`. The `scheduled_date_local[criteria]/[value]`
filter works as a cursor (`>= 2026-07-29` → 11,055). This is the per-send table,
pollable — no webhook needed, exactly as planned.

## Campaign naming — hazards for the client matcher

95 campaigns. Dominant pattern: `<Client>[ N] + <Persona> + <Segment>`.

- `Copy of LIV Indy Realty 4 + Nicole + MIBOR` — **client name is not always a
  prefix**, which validates matching anywhere in the name.
- `Kelly + Co + Nicole + BRIGHT` — the client name **contains the `+`
  separator**. Splitting on `+` is wrong.
- `Rise + Nicole + SRAR` vs `Rise Real Estate Antelope 2` vs
  `Rise Real Estate Tujunga` vs `TEST ONBOARDING Rise Realty Of Florida LLC` —
  **deeply nested names**; longest-match-wins is load-bearing.
- `The RE Home Group of Douglas Realty` vs `Douglas Elliman LA` — whole-token
  matching keeps these apart; substring matching would not.
- `Jeff Cook Real Estate LPT Realty  2` — **double space**; normalisation must
  collapse whitespace.
- `SERHANT. PA` / `SERHANT PA tempo` — punctuation must normalise away.
- Non-client campaigns needing an "Internal" bucket: `Template Zillow Flex -
  CST/EST/MST/PST`, `Interested ZF - *` (4), `Not Interested - All Clients`,
  `Front Range Realty - OpsLabs Test`, `Test Client`, `New Test Client`.

---

# Follow-up time + Positive — resolved 2026-07-31

## `GET portal.brokerstaffer.com/api/metrics/follow-up-time`

```json
{ "ok": true, "timezone": "America/New_York",
  "business_hours": "Mon–Fri 09:00–17:00",
  "days": [ { "date": "2026-07-28", "median_seconds": 480,
              "median_human": "8m", "sample_size": 41 } ] }
```

92 days (2026-03-05 .. 2026-07-30), 2,193 samples, 7.6 KB. Requires the
service-role token as `?token=` — the portal host gate rewrites untokened
`/api/*` to an HTML page with **status 200**, so a bare curl looks like it
"works" while returning markup. Never treat a 200 from that host as success
without checking the content type.

**It ignores `from`/`to`** (and `start_date`/`end_date`) and always returns all
92 days. Payload is small enough that fetching whole and filtering locally is
fine.

### ⚠️ These are PRE-AGGREGATED DAILY MEDIANS, which the KPI band cannot sum

A median over a date range is **not** derivable from per-day medians. Averaging
them is wrong; so is taking the median of them. With `sample_size` we can
compute a sample-weighted approximation, but it is an estimate, not the median.

Two honest options, in preference order:
1. Ask for a range-scoped variant returning ONE median for `[from, to]`
   (cheapest fix, and exact).
2. Render a **sample-size-weighted median of daily medians** and label the tile
   as approximate.

Also note this metric is **business-hours adjusted** (Mon–Fri 09:00–17:00
America/New_York), so it is not comparable with Median Reply Time, which is raw
elapsed time. That difference must be surfaced in the UI or the two tiles will
be read as if they were the same measure.

## Positive — use EmailBison's `interested` after all

Re-measured 2026-07-31: `GET /api/replies?status=interested` now returns **105**,
up from **30** the previous day. The team is actively marking replies, so the
flag is live and backfilling. Positive is therefore
`replies WHERE interested = true`, synced per campaign.

⚠️ **Two EmailBison counters disagree and must not be mixed:**

| Source | Value |
|---|---|
| `SUM(campaign.interested)` over all 95 campaigns | **29** |
| `GET /api/replies?status=interested` (reply rows) | **105** |
| per-campaign `/{id}/replies?status=interested` (c75=11, c55=10, c91=3, …) | consistent with 105 |

`campaign.interested` appears to count something else (likely distinct leads, or
a stale cached counter). **Use the reply-level count** — it is the one that can
be filtered to a date range, and it agrees with the per-campaign endpoint.

The `sentiment` / `sentiment_source` columns in migration 003 stay: they let a
classifier be layered on later without a migration, and `sentiment_source`
records whether a row came from `eb_interested` or something else.

## Campaign write endpoints (probed 2026-07-31, campaign 69 — a zero-send test campaign)

| Endpoint | Result |
|---|---|
| `PATCH /api/campaigns/{id}/pause` | 200, empty body. `completed` → `paused`. |
| `PATCH /api/campaigns/{id}/resume` | 200, returns the full campaign object. `paused` → **`queued`**. |
| `PATCH /api/campaigns/{id}/archive` | documented; not probed against a live campaign |
| `POST /api/campaigns/{id}/duplicate` | documented; not probed |
| `DELETE /api/campaigns/bulk` | body `{campaign_ids: []}` |
| `PATCH /api/campaigns/{id}/update` | name, max_emails_per_day, max_new_leads_per_day, plain_text, open_tracking, reputation_building, can_unsubscribe, include_auto_replies_in_stats, sequence_prioritization |
| `POST /api/tags/{attach-to,remove-from}-campaigns` | body `{tag_ids: [], campaign_ids: []}` — the only genuinely bulk write besides delete |

**`resume` does not restore the previous status — it queues the campaign to send.**

This is the most important line on this page. Campaign 69 was `completed` with
0 sent and 828 leads attached; one `resume` call put it in `queued`, i.e. ready
to email 828 real people. There is no "undo" that returns it to `completed` —
the only safe recovery is `pause`.

Two consequences for the UI, both load-bearing:

1. **Resume is only offered for campaigns that are actually `paused`.** Offering
   it on a `completed` campaign reads as "un-hide this row" and is in fact
   "start sending to everyone in it".
2. **The confirmation names the lead count.** "Resume — up to 828 leads may
   receive email" is a different decision from "Resume".

There is also a `queued` status that appears nowhere in the workspace snapshot
(`draft`, `launching`, `active`, `paused`, `completed`, `archived`), because it
is transient. Status vocabularies must include it or a resumed campaign renders
as an unknown state.

### Pause is not always sticky

Observed while wiring the actions UI. Campaign 69 was paused at 18:59 and
`GET /api/campaigns/69` confirmed `paused`. The `sync-entities` run minutes
later read it back as `active`, and the audit log recorded `before_state:
{status: "active"}` when it was paused again at 19:28.

So EmailBison can re-activate a campaign after a pause — presumably its own
scheduler picking up a campaign that still has a sending schedule attached.

Two consequences:

1. **Never treat an action's own response as the new state.** The UI refetches
   the list from the cache after every action, and the cache is written from
   what EmailBison reported, not from what we asked for. Optimistic updates
   would show "paused" on a campaign that is sending.
2. **A pause may need re-applying.** Worth surfacing if it recurs; for now the
   audit log makes it visible, because `before_state` records what the campaign
   actually was at the moment of each action rather than what we last saw.

Final state of campaign 69 after probing: `paused`, `emails_sent: 0`,
`total_leads_contacted: 0` — nothing was sent. It was `completed` before the
probe, and no endpoint restores that status.

## Sequence + lifecycle writes (probed 2026-08-01 on campaign 13, "Front Range Realty - OpsLabs Test")

Campaign 13 was snapshotted, probed, and restored byte-identical. A throwaway
duplicate (129) absorbed the destructive tests and was deleted.

| Call | Behaviour |
|---|---|
| `POST /api/campaigns/v1.1/{id}/sequence-steps` | **Appends.** Existing steps untouched. Body `{title, sequence_steps: [...]}`. |
| `PUT /api/campaigns/v1.1/sequence-steps/{sequence_id}` | **Updates only the steps you list, by `id`.** |
| `DELETE /api/campaigns/sequence-steps/{step_id}` | 200 `{success: true}`. Deleting again → 404 `Record not found.` |
| `POST /api/campaigns/{id}/duplicate` | Creates a `draft` copy named `Copy of <name>`, 0 leads, with a **deep-copied sequence**. |
| `PATCH /api/campaigns/{id}/archive` | 200. |
| `DELETE /api/campaigns/{id}` | 200 `"...has been queued for deletion."` — **asynchronous**. |

### There is no "replace the sequence" call

`PUT` requires an `id` on every step (`The sequence_steps.1.id field is
required.`) so it cannot create, and it does **not** delete the steps you omit —
verified by PUTting a one-step body against a two-step sequence and getting two
steps back.

So §9.4's **Replace** mode is necessarily `DELETE` every existing step, then
`POST` the new ones: several calls, no transaction, and a window in which the
target campaign has no sequence at all. That is precisely why the spec demands
the previous sequence be recorded before anything is overwritten — it is the
only way back.

**Append** mode, by contrast, is a single `POST`.

### The duplicate response reports the SOURCE's sequence_id

`POST /api/campaigns/13/duplicate` returned `{"id": 129, "sequence_id": 9}` —
but campaign 129's actual sequence is **117**, with its own step ids. Sequence 9
belongs to campaign 13.

Anything that duplicates a campaign and then edits `response.sequence_id` would
silently rewrite the ORIGINAL campaign's emails. Always re-fetch
`/api/campaigns/v1.1/{newId}/sequence-steps`.

### Archive is one-way

There is no unarchive, restore or untrash endpoint in the whole API. Archiving
twice leaves it archived, and `resume` on an archived campaign fails. Treat
Archive as terminal in the UI and confirm it like one.

### `thread_reply: true` rewrites the subject

A step posted with `email_subject: "PROBE STEP 2"` and `thread_reply: true` came
back as `"Re: PROBE STEP 2"`. EmailBison owns the prefix, so an editor must not
show the user's typed subject as saved — re-read it.

### Two more real error messages, now surfaced verbatim

- `resume` on an incomplete campaign → 400: *"This campaign is incomplete and
  cannot be launched. Campaigns should have a completed sequence and schedule,
  and should have leads and sender emails."*
- `PUT` sequence-steps without ids → 422: *"The sequence_steps.1.id field is
  required."*

Both arrive as `{"data": {"success": false, "message": ...}}` with a correct
non-2xx status, which is why `describeEmailBisonError` unwraps `data`.
