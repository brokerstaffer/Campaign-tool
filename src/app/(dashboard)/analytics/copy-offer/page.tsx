import { ComingSoon } from "@/components/analytics/coming-soon";

export default function CopyOfferPage() {
  return (
    <ComingSoon
      title="Copy & Offer"
      description="Which subject lines, openings, CTAs and offers actually produce positive replies — grouped by any of seven copy dimensions."
      blockedOn="the copy-dimension tables and the per-step tagging UI"
    />
  );
}
